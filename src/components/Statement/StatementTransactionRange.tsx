import { Transaction } from "@/types/Account/Transaction";

export default function StatementTransactionRange({
  transactions,
}: {
  transactions: Transaction[];
}) {
  return <div>This is a transaction</div>;
}
